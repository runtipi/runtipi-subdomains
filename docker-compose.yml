services:
  runtipi-subdomains:
    image: ghcr.io/runtipi/runtipi-subdomains:latest
    container_name: runtipi-subdomains
    restart: unless-stopped
    ports:
      - 80:3000
    volumes:
      - ./data:/data
